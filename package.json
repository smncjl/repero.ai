{
  "name": "repero-ai-landing-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "npm run brand:generate && npm run brand:validate",
    "build": "astro build",
    "preview": "astro preview",
    "test:blog-build": "node scripts/test-blog-build.mjs",
    "test:fr-product-pages": "node scripts/test-fr-product-pages.mjs",
    "test:legal-publication": "node scripts/test-legal-publication.mjs",
    "verify:no-redirects": "node scripts/verify-no-redirects.mjs",
    "check": "npm run build && npm run verify:no-redirects",
    "brand:generate": "node scripts/generate-brand-assets.mjs",
    "brand:validate": "node scripts/validate-brand-assets.mjs"
  },
  "dependencies": {
    "astro": "^5.8.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
